let n;
let x;

function potencia(n, x = parseInt (n)){ 
    return p = Math.pow(n, x); 
    //let x = Math.pow(n, x);     
}

console.log(potencia(2) );
console.log(potencia(3) );
console.log(potencia(3,4) );
console.log(potencia(2,10) );